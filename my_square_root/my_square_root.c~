int my_square_root (int nb)
{
  int i;
  if (nb >= 0)
    {
      for (i = 0 ; i < nb / 2 ; i++)
	{
	  if (i * i == nb)
	    {
	      return i;
	    }
	}
    }
  return 0;
}
#include <stdio.h>
int main(){int res = my_square_root(64);
  printf("prime = %d", res); return (0);  }
